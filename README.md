# Defiant Contracts

This repository contains versioned, immutable contract definitions shared across Defiant systems.

## Rules

- Contract files are read-only.
- No local modifications are allowed.
- Any change requires a version bump and coordinated update across all consumers.
- Schema and checksum files must remain byte-identical across all repositories.